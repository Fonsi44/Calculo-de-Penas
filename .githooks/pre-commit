#!/bin/sh

branch="$(git symbolic-ref --quiet --short HEAD 2>/dev/null)"

if [ "$branch" != "main" ]; then
  echo "ERROR: este repositorio solo permite commits directos en main." >&2
  echo "Rama activa: ${branch:-HEAD separado}" >&2
  exit 1
fi
