## Plan FINAL aprobado — Saneamiento SEO Ahrefs Fase 1 (con ajustes del usuario)

### Diagnóstico confirmado (validado contra código + DB)
1. **11 URLs 4XX**: 3 provienen del body HTML del post `como-obtener-rtn-personas-empresas-honduras` (en DB `blog_posts`, enlaces a `/articulos/*` inexistentes). Los 8 restantes (slugs doble-prefijo `/blog/tributario/blog/...`, `/contacto-tegucigalpa`, `/servicios/gestoria-ambiental-corporativa`) **no están en código ni DB** — son artefactos históricos de Ahrefs/GSC.
2. **3XX internos**: posts laborales/penales/notariales y `/contacto` que dependen de redirects de `next.config.ts`. `/contacto` → 301 → `/solicitar-consulta` (confirmado L139).
3. **Contradicción X-Robots-Tag**: `next.config.ts:67-69,323` envía `X-Robots-Tag: index, follow` global (vía regla catch-all `/:path*`) a TODAS las páginas públicas, incluidas las noindex (legales, `?tag=`, `?month=`, `?page=`). Contradice el meta robots por-página.
4. **`/intranet/admin`**: impreso con `rel=nofollow` en `public-header.tsx:64-72` (header global, todas las páginas).
5. **Tags pie de post**: `blog/[categoria]/[slug]/page.tsx:463` sin `rel=nofollow` (inconsistencia con sidebar).

### Cambios (ajustados a las precisiones del usuario)

#### A. `next.config.ts` — Eliminar X-Robots-Tag global; noindex solo para legales
- **Eliminar** `robotsHeader` estático (L67-69) de la regla catch-all `/:path*` (L323): las páginas indexables quedan SIN `X-Robots-Tag` (la metadata por-página + sitemap es la autoridad).
- **Añadir** regla `headers()` con matcher para las 6 rutas legales explícitas (`/terminos`, `/aviso-legal`, `/politica-privacidad`, `/politica-cookies`, `/politica-editorial`, `/disclaimer`) → `X-Robots-Tag: noindex, follow` (consistente con su meta robots).
- Respeta `noindexActive` global (staging): si está activo, todas las rutas siguen con noindex.

#### B. `next.config.ts` — Redirects 301 de red de seguridad (5 rutas con destino canónico claro)
- `/articulos/declaracion-isr-personas-naturales` → `/blog/tributario/impuesto-renta-personas-fisicas-honduras`
- `/articulos/facturacion-electronica-honduras` → `/blog/tributario/facturacion-electronica-requisitos-sar`
- `/articulos/isv-en-honduras` → `/blog/tributario/isv-impuesto-venta-tasas-obligaciones-honduras`
- `/contacto-tegucigalpa` → `/solicitar-consulta`
- `/servicios/gestoria-ambiental-corporativa` → `/servicios-juridicos/ambiental-regulatorio`
- Los slugs doble-prefijo `/blog/tributario/blog/...` **NO se redirigen** (artefactos; sin referencia real en código/DB). Se documenta.

#### C. `scripts/fix-internal-redirects.ts` — Ampliar con mapa de URLs rotas
- Añadir mapa de reescritura: `/articulos/*` (3 URLs) → posts canónicos. Y `/contacto` → `/solicitar-consulta` (directo, sin redirect).
- Mantener patrón: dry-run por defecto, idempotente, backup-required, no `--aplicar` en este turno.
- El reporte mostrará: post afectado, campo `body`, URLs finales 200.

#### D. `components/marketing/public-header.tsx` — Retirar `/intranet/admin` del HTML público
- Eliminar el bloque `Link href="/intranet/admin"` (L64-72). La intranet sigue existiendo, protegida por robots.ts + auth; accesible por URL directa.

#### E. `app/(public)/blog/[categoria]/[slug]/page.tsx:463` — `rel="nofollow"` en tags pie
- Añadir `rel="nofollow"` a los enlaces `?tag=` del pie (coherente con sidebar).

#### F. `scripts/seo-ahrefs-audit.mjs` — NUEVO validador Fase 1
- Lee CSV UTF-16LE/TSV de `ahrefs/`, autodetecta columnas. Reporta 4XX, enlaces a 3XX, noindex con señales contradictorias, noindex en sitemap, `/intranet/admin` en componentes públicos.
- **exit 1** si: enlaces internos a 4XX/3XX activos, o `/intranet/admin` en HTML público.

#### G. `package.json` — `"seo:ahrefs": "node scripts/seo-ahrefs-audit.mjs"`

#### H. Documentación: `CHANGELOG.md` (siempre), `README.md` (solo si existe sección scripts SEO).

### NO se hace
- No `--aplicar` sobre DB (se entrega reporte; el usuario decide).
- No push, no cambios destructivos, no `|| true`, no tocar auth/intranet/visual público salvo los cambios quirúrgicos señalados.

### Validaciones
`npm run lint` · `npx tsc --noEmit` · `npm test` · `npm run build` · `npm run seo:ahrefs` · dry-run `fix-internal-redirects`.