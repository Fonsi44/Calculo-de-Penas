## Plan — Saneamiento SEO Ahrefs Fases 2–7 (alcance ajustado a CSV disponibles)

### Contexto
Solo existen los 6 CSV de la Fase 1. Las Fases 2 (titles/meta) y 3 (orphans) del prompt original dependen de CSV inexistentes (Title too long, Meta description, orphan-pages, etc.). Según la decisión del usuario y el protocolo (R4/R11/R12), se trabaja **solo lo verificable** y el resto se documenta como PENDIENTE.

### Hallazgos validados contra DB (149 posts publicados)
- **Duplicación de marca**: 0 casos (ni 2+ ocurrencias, ni marca en medio). `stripDuplicateBrand` mitiga correctamente.
- **Title con defecto editorial auto-evidente**: 1 caso — `habilitacion-clinicas-hospitales` tiene `title = "...ARSA | [Tu Empresa]"` (placeholder sin reemplazar).
- **Titles largos (>65 chars)**: 6 — PENDIENTE del CSV de Ahrefs.
- **Meta descriptions largas (>165 chars)**: 12 — PENDIENTE del CSV de Ahrefs.
- **Canonical**: estrategia sólida, sin incidencias. Post→landing vía `canonical_url` en DB (intencional).
- **Sitemap**: 213 URLs, 0 noindex/legales/intranet/filtros. Correcto.
- **Orphans**: 9 sub-slugs + 6 landings secundarias sin enlace global, pero **sin CSV orphan-pages** para confirmar. PENDIENTE.

### Cambios a realizar

#### 1. `lib/blog-db.ts` o script de corrección — Title `[Tu Empresa]`
El title del post `habilitacion-clinicas-hospitales` contiene `| [Tu Empresa]` (placeholder sin reemplazar). Corrección quirúrgica en DB: eliminar el sufijo `| [Tu Empresa]` (el template ya añade la marca). Vía script idempotente con backup-required (patrón `fix-internal-redirects`). **No se reescribe contenido jurídico, solo se elimina el placeholder.**

#### 2. `scripts/seo-ahrefs-audit.mjs` — Ampliar validador (Fase 6)
Añadir dos chequeos nuevos contra la DB (decisión del usuario: SÍ validar titles duplicados de marca vía DB):
- **Titles duplicados de marca**: cuenta posts cuyo `title`/`meta_title` contiene "Pineda y Asociados" 2+ veces (duplicación real). Exit 1 si encuentra.
- **Titles con placeholder editorial**: detecta `[Tu Empresa]` y placeholders similares sin reemplazar. Exit 1 si encuentra.
- **Metadata crítica ausente**: posts publicados sin `title` o sin `meta_description`/`description`. Exit 1 si encuentra.
No se valida longitud (sin CSV de Ahrefs, sería heurística no solicitada). El script mantiene sus chequeos existentes (4XX, 3XX, noindex contradictorios, /intranet/admin público).

#### 3. `package.json` — sin cambios (el script ya existe como `seo:ahrefs`)

#### 4. `CHANGELOG.md` — entrada de Fases 2–7 (siempre)
#### 5. `README.md` — ampliar descripción de `seo:ahrefs` con los nuevos chequeos

#### 6. Documentación de PENDIENTES
En `CHANGELOG.md`, sección explícita "PENDIENTE — requiere CSV adicionales de Ahrefs":
- Title too long / Meta description too long / Duplicate title / Missing (6 titles largos, 12 meta largas).
- Orphan pages / Low inlinks (9 sub-slugs + 6 landings secundarias).
- Duplicate content / Thin content / Recursos rotos.

### NO se hace
- No tocar orphans sin CSV (decisión del usuario).
- No acortar titles/meta por longitud sin CSV (decisión del usuario).
- No convertir tags/meses/paginación/legales en indexables.
- No tocar auth/intranet.
- No push, no cambios destructivos sin backup.

### Validaciones
`npm run lint` · `npx tsc --noEmit` · `npm test` · `npm run build` · `npm run seo:ahrefs` · `seo-content-audit` · dry-run `fix-internal-redirects`.